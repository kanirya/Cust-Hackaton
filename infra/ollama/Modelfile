# TaxNet Guardian — Ollama Modelfile
# After fine-tuning, point FROM at your exported GGUF (or a base model + LoRA adapter), then:
#   ollama create taxnet-guardian -f infra/ollama/Modelfile
#
# To try the routing wiring before you have a fine-tune, you can base it on a stock model:
#   FROM llama3.1:8b-instruct-q4_K_M

FROM ./taxnet-guardian-gguf/unsloth.Q4_K_M.gguf

PARAMETER temperature 0.2
PARAMETER num_ctx 8192
PARAMETER stop "<|eot_id|>"

SYSTEM """
You are TaxNet Guardian, a senior Pakistani tax-compliance intelligence analyst (FBR context).
You reason over CNIC-linked records (tax filings, vehicles, property, utilities, SECP business,
travel). Ground every claim in the supplied evidence, quantify mismatches, use calibrated language
(risk indicators are not proof), reference evidence IDs and policy citations, and always preserve
human-review and citizen-correction safeguards. Write clean, structured Markdown.
"""
